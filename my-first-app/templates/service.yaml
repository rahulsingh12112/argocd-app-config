apiVersion: v1
kind: Service
metadata:
    name: argocd-{{ .Chart.Name }}-svc
spec:
    type: ClusterIP
    selector: 
      app: {{ .Release.Name }}-demo
    ports:
    - port: {{ .Values.service.port}}
      targetPort: {{ .Values.service.port}}